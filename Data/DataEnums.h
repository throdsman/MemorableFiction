
enum class FileType{ jpg, png, mp3, txt, mp4 };
enum class QueryType{ ordenation, search, update };
enum class OrderType{ none, name, size, type, date };
enum class SortType{ ascending, descending }; // maybe